exposed device communication
    when connection closes, state is cleaned on both ends
dynamic user frontend
    ui types scafolding
    reset button in ui
    server exposed device pool logic
    client gets informed if their device is removed
    local highlights currently used devices
sessions
    login from local starts a separate session
    view and stop session enpoints
general security
    jwt, https, wss, log in and out with password
    proper logout on shutdown
